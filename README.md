### WHEEL PACKAGE 

A wheel package is a built distribution format for Python.
It simplifies the process of installing and distributing Python projects. By creating your own wheel package, you can package your code into a reusable and shareable format that others (or even yourself) can easily install and use.
